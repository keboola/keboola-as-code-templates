{
  local shop = Input("ex-shoptet-permalink-shop-name"),
  name: if shop != null && shop != "" then shop + " Ads & Sales Performance pipeline" else "Shoptet Ads & Sales Performance pipeline",
  isDisabled: false,
}
